# q
Simple tui for oneshot answers with tool calls

## Usage
With ollama running on the localhost and `deno` on the path, run the
following in the cwd
``` shell
deno run --allow-env --allow-net main.ts
```
